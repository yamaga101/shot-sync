# shot-sync — 2026-05-06

## 16:52 (v0.2.0 ship session)

### Done

- v0.1.9 → v0.2.0 ship。前 session の handoff (`2026-05-06-handoff.md`) の
  TODO 1 / 2 を実装。
- TODO 1: cold start で `autoSyncEnabled=true` でも service が落ちたままの
  UX bug。`MainViewModel.refreshPermissions` (Activity onResume hook) で
  permissions allGreen 時に `ensureAutoSyncRunning` を呼ぶ形で救済。
  idempotent な `UploadForegroundService.start` (既に live なら
  onStartCommand のみ走る、START_STICKY)。診断ログに痕跡残し。
- TODO 2: `DEFAULT_CAMERA_DRIVE_FOLDER_ID = "12y4EpdE..."`。
  `cameraDriveFolderId` Flow / `snapshot()` を未設定/空欄時に default
  返すよう揃える。明示保存済み user は touch しない。UI placeholder と
  説明文も更新。
- 配布: `assembleDebug` / k35 check pass / commit ad722d4 push /
  GitHub Releases v0.2.0 公開。

### Why

- 前 session が ev-manager session の CWD から走ったため (operating.md §7.4
  session isolation 違反) handoff doc に v0.2.0 TODO を残してあった。
  今 session は `cc shot-sync` で開いて宣言通り消化。

### Changed

- `app/build.gradle.kts` (versionCode 10→11 / versionName 0.1.9→0.2.0)
- `app/src/main/java/jp/gmail/yamaga101/shotsync/Settings.kt`
  (DEFAULT_CAMERA_DRIVE_FOLDER_ID 追加 + Flow / snapshot 同期)
- `app/src/main/java/jp/gmail/yamaga101/shotsync/MainActivity.kt`
  (placeholder 文言更新)
- `app/src/main/java/jp/gmail/yamaga101/shotsync/ui/MainViewModel.kt`
  (`ensureAutoSyncRunning` + DiagnosticsLog 連携)
- `package.json` (version 同期、SoT 統一は別 task)

### Next

- 実機 install → cold start 救済 / Camera default の動作確認 (user 担当)
- 通った場合: handoff TODO 3 群 (polish、優先度低)
  - folder 削除 / 権限剥奪 / ID 重複の起動時検査
  - Drive quota 監視
  - EXIF strip
  - Drive `appProperties` で source / upload timestamp tag 付け
- v0.2.x の SoT 統一: `package.json` ↔ `build.gradle.kts` の version
  drift が起きうるので build script 経由で 1 箇所に集約する (standards.md §4.3)。

### Recovery Test (§6.4)

1. **CLAUDE.md** → ✅ `shot-sync/CLAUDE.md` に carve-out 制約 / build/run /
   k35 check / Release 配布 URL ルールあり。新人がここから入れる。
2. **docs/** → ✅ 設計判断は `docs/specs/system-yamaga101-carveout.md`、
   v0.1.9 までの経緯は `docs/logs/2026-05-06-handoff.md`、v0.2.0 は本 log。
   commit message と組み合わせれば「なぜ」追える。
3. **MEMORY.md** → MEMORY.md は本 PJ には作っていない (個人 carve-out で
   小規模、CLAUDE.md Quick Reference + handoff doc + 本 log で十分)。
   project menu / EOD では本 log を Current Work 代わりに参照する。
4. **git log** → ✅ commit ad722d4 の message に handoff TODO 1/2 の
   症状・原因・修正方針が full text で書かれている。

### Report Impact

(該当なし — 個人 carve-out / yamaga101 配下 / 業務 visibility なし)
