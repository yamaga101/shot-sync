{
  "name": "shot-sync",
  "private": true,
  "version": "0.3.0",
  "description": "Auto-sync /Pictures/Screenshots/ to Google Drive (yamaga101 carve-out)",
  "scripts": {
    "setup-hooks": "git config core.hooksPath .githooks && chmod +x .githooks/pre-commit scripts/check-no-k35.sh scripts/release-apk.sh",
    "check-k35": "bash scripts/check-no-k35.sh",
    "build": "./gradlew :app:assembleDebug",
    "release-apk": "bash scripts/release-apk.sh",
    "ship-apk": "npm run build && npm run release-apk"
  }
}
